import request from "@/utils/requests.js";
/**
 * 默认导出一个空对象，用于在其他地方被导入和使用。
 * 这个空对象可以在后续开发中被逐步充实，以避免在一开始就需要定义大量属性或方法。
 *
 * @export 导出这个对象，使得它可以在其他模块中被导入。
 * @default 指定这个导出为默认导出，允许其他模块使用默认导入语法来引用它。
 */
export default {
}

